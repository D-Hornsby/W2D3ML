function charCount(str, letter){
  var letter_Count = 0;
  for (var postion) = 0; position < str.length; position++)
{
    if (str.charAt(position) == letter){
      letter_Count +=1;
    }
    return letter_Count
  }
  console.log(char_count('please work'))

  //needs more work. not sure whats wrong  
